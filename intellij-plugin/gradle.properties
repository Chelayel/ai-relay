org.gradle.jvmargs=-Xmx2g -XX:+UseParallelGC
org.gradle.caching=true
org.gradle.configuration-cache=false
kotlin.stdlib.default.dependency=false

# The plugin's own version: bump it when the plugin (or the chat page) changes.
# Publishing happens on the next tag when the Marketplace has an older one.
pluginVersion=1.8.8
