org.gradle.jvmargs=-Xmx2g -XX:+UseParallelGC
org.gradle.caching=true
org.gradle.configuration-cache=false
kotlin.stdlib.default.dependency=false

# A tag builds and publishes the plugin at the tag's version (release.yml);
# this one only labels builds off main. Keep it at the next release's.
pluginVersion=2.1.1
